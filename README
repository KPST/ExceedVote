ExceedVote project of KPST group is a Voting system that have to used in exceed camp.

doc/ contain document

demo/ contain demo of program

lib/ contain libary that use in this program

sql/ contain sql 

scr/ contain code of program

WebContent/ contain code of WebContent after 2012/11/24 

ExceedUI/ contain draw of UI

template/ contain UI template that have to edit

web/ contain beta stege web version

dist/ contain final version web app war file

INSTALL.txt contain document how to install the war file